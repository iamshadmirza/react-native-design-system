import {Meta, Story, Preview, Props} from '@storybook/addon-docs/blocks';
import {withKnobs, select, text, boolean, color} from '@storybook/addon-knobs';
import {action} from '@storybook/addon-actions';
import Stack from '../Layout/Stack';
import Feather from 'react-native-vector-icons/Feather';
import Avatar from './Avatar';
import {sizeArray} from '../util/prop-types';

<Meta title="Avatar" component={Avatar} />

# Avatar

Avatars are used to represent a user and can contain photos or text.

# Usage

```js
import {Avatar} from 'react-native-design-system';

<Avatar
  title="MD"
  source={{uri: 'image_url_here'}}
  onPress={() => this.handlePress()}
/>;
```

# Props

<Props of={Avatar} />

# Sizes

As usual, 9 sizes are available.

<Preview>
  <Story name="sizes">
    <Stack direction="horizontal">
      <Avatar size="5xl" title="MD" onPress={action('pressed')} />
      <Avatar size="4xl" title="MD" onPress={action('pressed')} />
      <Avatar size="3xl" title="MD" onPress={action('pressed')} />
      <Avatar size="2xl" title="MD" onPress={action('pressed')} />
      <Avatar size="xl" title="MD" onPress={action('pressed')} />
      <Avatar size="lg" title="MD" onPress={action('pressed')} />
      <Avatar size="md" title="MD" onPress={action('pressed')} />
      <Avatar size="sm" title="MD" onPress={action('pressed')} />
      <Avatar size="xs" title="MD" onPress={action('pressed')} />
    </Stack>
  </Story>
</Preview>

# Types

There are mainly three types og Avatar: `default`, `square` and `rounded`. All props accept `boolean` value.

<Preview>
  <Story name="types">
    <Stack direction="horizontal">
      <Avatar title="MD" onPress={action('pressed')} />
      <Avatar square title="MD" onPress={action('pressed')} />
      <Avatar rounded title="MD" onPress={action('pressed')} />
    </Stack>
  </Story>
</Preview>

# Title

`title` accepts a string. Passing upto 3 character (maximum) is recommended.

<Preview>
  <Story name="title">
    <Avatar title="MD" />
  </Story>
</Preview>

# Source

`source` is same as Image component. You can pass a `required` image or a `uri` object.

<Preview>
  <Story name="source">
    <Avatar source={{uri: 'https://www.github.com/iamshadmirza.png'}} />
  </Story>
</Preview>

# Editable

`editable` is a boolean value which adds an edit button to the bottom. You just have to pass one `onPress` function and the `Avatar` is clickable.

<Preview>
  <Story name="editable">
    <Stack direction="horizontal">
      <Avatar title="MD" editable={true} onPress={action('pressed')} />
      <Avatar
        source={{uri: 'https://www.github.com/iamshadmirza.png'}}
        editable={true}
        onPress={action('pressed')}
      />
    </Stack>
  </Story>
</Preview>

# Playground

Play with Avatar in Canvas mode.

<Story name="playground" parameters={{decorators: [withKnobs]}}>
  <Avatar
    size={select('size', sizeArray, 'md')}
    title={text('title', 'MD')}
    editable={boolean('editable', true)}
    onPress={action('pressed')}
    editIconColor={color('editIconColor', '#78909c')}
  />
</Story>
