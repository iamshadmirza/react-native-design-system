import {Meta, Story, Preview, Props} from '@storybook/addon-docs/blocks';
import {withKnobs, select} from '@storybook/addon-knobs';
import {action} from '@storybook/addon-actions';
import Button from '../Button/Button';
import Box from './Box';

<Meta title="Box" component={Box} />

# Box

Box is a simple layout container which accepts a child and provide padding around it. The child is centered by default.

# Usage

```js
import {Box} from 'react-native-design-system';

<Box size="lg">
  <Button>Press Me</Button>
</Box>;
```

# Props

<Props of={Box} />

# Space

Customize padding for the child. `space` prop accepts one of the given values: `none`, `xxsmall`, `xsmall`, `small`, `medium`, `large`, `xlarge`, `xxlarge`

<Preview>
  <Story name="space">
    <Box size="lg">
      <Button>Press Me</Button>
    </Box>
  </Story>
</Preview>

# Background

You can also pass a `background` color. Color string should be present inside `brandColor` of theme.

<Preview>
  <Story name="background">
    <Box background="backgroundLight">
      <Button>Press Me</Button>
    </Box>
  </Story>
</Preview>

# Playground

Play with Box in Canvas mode.

<Story name="playground" parameters={{decorators: [withKnobs]}}>
  <Box
    space={select(
      'space',
      ['none', 'xs', 'xs', 'sm', 'md', 'lg', 'xl', '2xl'],
      'md',
    )}
    background={select('background', [
      'backgroundDark',
      'backgroundLight',
      'foreground',
      'white',
      'disabled',
      'semitransparent',
      'transparent',
      'black',
    ])}>
    <Button>Press Me</Button>
  </Box>
</Story>
