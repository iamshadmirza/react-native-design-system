import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs } from "@storybook/addon-knobs";
import {useState} from 'react'
import { action } from '@storybook/addon-actions';
import Stack from '../Layout/Stack';
import AntDesign from 'react-native-vector-icons/AntDesign';
import MenuAddButton from './MenuAddButton';
import ExampleMenuButton from './MenuAddButton.example.js';

<Meta title="Design System|MenuAddButton" component={MenuAddButton} />

# MenuAddButton
A `MenuAddButton` is a clickable button to carry out actions like adding and removing items from cart.

# Usage
```js
import { MenuAddButton } from 'react-native-design-system';

<MenuAddButton
  onIncrement={() => handleIncrement())}
  onDecrement={() => handleDecrement())}
  count={this.state.count}
/>
```

# Props
<Props of={MenuAddButton} />

# Count
Pass an `Integer` value in `count` prop as number of items.

<Preview>
  <Story name="count">
    <Stack>
      <MenuAddButton
        onIncrement={action('pressed')}
        onDecrement={action('pressed')}
        count={0}
      />
      <MenuAddButton
        onIncrement={action('pressed')}
        onDecrement={action('pressed')}
        count={5}
      />
      <MenuAddButton
        onIncrement={action('pressed')}
        onDecrement={action('pressed')}
        count={25}
      />
      <MenuAddButton
        onIncrement={action('pressed')}
        onDecrement={action('pressed')}
        count={100}
      />
    </Stack>
  </Story>
</Preview>

# Disabled
`disabled` accepts a boolean value and restricts touch response. 

<Preview>
  <Story name="disabled">
    <MenuAddButton
      disabled
      onIncrement={action('pressed')}
      onDecrement={action('pressed')}
      count={0} 
    />
  </Story>
</Preview>

# Size
There are 7 different size available. You may not need all of them but it doesn't hurt to have them just in case. 😅

<Preview>
  <Story name="size">
    <Stack>
      <MenuAddButton size="xxsmall"
        onIncrement={action('pressed')}
        onDecrement={action('pressed')}
        count={0}
      />
      <MenuAddButton size="xsmall"
        onIncrement={action('pressed')}
        onDecrement={action('pressed')}
        count={2}
      />
      <MenuAddButton size="small"
        onIncrement={action('pressed')}
        onDecrement={action('pressed')}
        count={10}
      />
      <MenuAddButton size="medium"
        onIncrement={action('pressed')}
        onDecrement={action('pressed')}
        count={50}
      />
      <MenuAddButton size="large"
        onIncrement={action('pressed')}
        onDecrement={action('pressed')}
        count={100}
      />
      <MenuAddButton size="xlarge"
        onIncrement={action('pressed')}
       onDecrement={action('pressed')}
        count={500}
      />
      <MenuAddButton size="xxlarge"
        onIncrement={action('pressed')}
        onDecrement={action('pressed')}
        count={2020}
      />
    </Stack>
  </Story>
</Preview>

# Custom icons
Pass custom icon as `plusIcon` and `minusIcon` of your choice.

<Preview>
  <Story name="custom">
    <MenuAddButton
      onIncrement={action('pressed')}
      onDecrement={action('pressed')}
      plusIcon={
        <AntDesign
          name="like2"
          size={24}
          color="black"
        />
      }
      minusIcon={
        <AntDesign
          name="dislike2"
          size={24}
          color="black"
        />
      }
      count={10}
    />
  </Story>
</Preview>

# Playground
Play with MenuAddButton in Canvas mode.

<Story name="playground" parameters={{ decorators: [withKnobs] }}>
  <ExampleMenuButton />
</Story>