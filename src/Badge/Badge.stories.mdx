import {Meta, Story, Preview, Props} from '@storybook/addon-docs/blocks';
import {
  withKnobs,
  select,
  number,
  boolean,
  color,
} from '@storybook/addon-knobs';
import {action} from '@storybook/addon-actions';
import Inline from '../Layout/Inline';
import Feather from 'react-native-vector-icons/Feather';
import Badge from './Badge';
import {sizeArray} from '../util/prop-types';

<Meta title="Badge" component={Badge} />

# Badge

Badges are small components typically used to communicate a number value or indicate the status of an item to the user.

# Usage

```js
import { Badge } from 'react-native-design-system';

<Badge
  onPress={() => this.handlePress())}
>
  10000
</Badge>
```

# Props

<Props of={Badge} />

# Sizes

As usual, there are 13 sizes available.

<Preview>
  <Story name="sizes">
    <Inline>
      <Badge size="9xl" onPress={action('pressed')}>
        10000
      </Badge>
      <Badge size="8xl" onPress={action('pressed')}>
        10000
      </Badge>
      <Badge size="7xl" onPress={action('pressed')}>
        10000
      </Badge>
      <Badge size="6xl" onPress={action('pressed')}>
        10000
      </Badge>
      <Badge size="5xl" onPress={action('pressed')}>
        10000
      </Badge>
      <Badge size="4xl" onPress={action('pressed')}>
        10000
      </Badge>
      <Badge size="3xl" onPress={action('pressed')}>
        10000
      </Badge>
      <Badge size="2xl" onPress={action('pressed')}>
        10000
      </Badge>
      <Badge size="2xl" onPress={action('pressed')}>
        10000
      </Badge>
      <Badge size="xl" onPress={action('pressed')}>
        1000
      </Badge>
      <Badge size="lg" onPress={action('pressed')}>
        100
      </Badge>
      <Badge size="md" onPress={action('pressed')}>
        50
      </Badge>
      <Badge size="sm" onPress={action('pressed')}>
        0
      </Badge>
      <Badge size="xs" onPress={action('pressed')}>
        -10
      </Badge>
    </Inline>
  </Story>
</Preview>

# Types

There are 3 types of Badges mainly: `default` which is round, `sqaure` and `mini`. The prop accept boolean value.

<Preview>
  <Story name="types">
    <Inline>
      <Badge onPress={action('pressed')}>10</Badge>
      <Badge square onPress={action('pressed')}>
        10
      </Badge>
      <Badge mini onPress={action('pressed')} />
      <Badge color="success" onPress={action('pressed')}>
        Featured
      </Badge>
      <Badge color="error" onPress={action('pressed')}>
        Deprecated
      </Badge>
    </Inline>
  </Story>
</Preview>

# Playground

Play with Badge in Canvas mode.

<Story name="playground" parameters={{decorators: [withKnobs]}}>
  <Badge
    size={select('size', sizeArray, 'md')}
    color={select('color', ['primary', 'secondary', 'tertiary'], 'primary')}
    children={number('children', 10)}
    square={boolean('square', false)}
    mini={boolean('mini', false)}
    onPress={action('pressed')}
  />
</Story>
